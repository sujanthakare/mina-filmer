import React from 'react';

import { Box, Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Box>
        <Typography>HOME</Typography>
      </Box>
    </Container>
  );
};

export default Home;
